name: 'Fix nvmrc setup-node'
description: Set up Node 

runs:
  using: 'composite'
  steps:
    - uses: mahabaleshwars/setup-node@feature/852-absolute-path
      with:
        node-version-file: ${{ github.action_path }}/.nvmrc
